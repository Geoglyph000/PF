<table class="table table-bordered table_border_radius genre-table">
  <thead>
    <tr>
      <th>タグで検索</th>
    </tr>
  </thead>

  <tbody>
    <% genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.name, genre_index_path(genre.id) %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%#貼り付け用%>
<%# render 'homes/genre', genres: @genres %>